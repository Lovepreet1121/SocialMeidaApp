// social-media-frontend/src/components/Home.jsx

import React from 'react';

const Home = () => {
  return (
    <div>
      {/* This component acts as a placeholder. The actual content (CreatePost and PostFeed)
          is now rendered directly within the Route in App.jsx.
          You can remove this file if you don't intend to use it for other specific home-page content. */}
    </div>
  );
};

export default Home;